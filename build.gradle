subprojects {
    repositories {
        maven { url maven_main_repo_url }
        maven { url maven_jboss_repo_url }
    }

}

configure(allprojects) {
    apply plugin: 'java'
    sourceCompatibility = 1.8
    targetCompatibility = 1.8
}

